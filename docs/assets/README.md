# Documentation assets

Working files that support the docs — architecture diagrams, exported wireframe
images, screenshots referenced from Markdown.

These are **not** served to users; anything the website ships (favicons, OG
images, fonts) belongs in `public/` instead. Keep filenames descriptive and
reference them with relative links from the relevant doc.
